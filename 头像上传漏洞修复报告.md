# 头像上传功能 — 漏洞分析与修复报告

> **项目名称：** SQL 注入学习平台  
> **新增功能：** 头像上传（`POST /upload`）  
> **报告日期：** 2026-07-21  
> **审计版本：** `sqli-vuln-app-complete` 分支 (`c7ff981`)

---

## 一、漏洞概述

| 项目 | 内容 |
|:---|:---|
| **漏洞类型** | 任意文件上传（Unrestricted File Upload） |
| **风险等级** | 🔴 **严重（Critical）** |
| **影响范围** | 服务器被完全控制，数据泄露，内网横向移动 |
| **CVSS 3.1 评分** | **9.8**（AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:H） |
| **是否存在修复** | ❌ 未修复（存在严重安全隐患） |

---

## 二、代码审计 — 漏洞逐条分析

### 🔴 漏洞 1：无文件类型校验（最严重）

**位置：** `app.py` — `upload()` 路由

```python
# ⚠️ 漏洞：直接保存，无任何检查
f = request.files["file"]
filename = f.filename
save_path = os.path.join(UPLOAD_FOLDER, filename)
f.save(save_path)
```

**危害：** 攻击者可上传任意格式文件，包括：
- PHP webshell（`shell.php`）
- JSP webshell（`shell.jsp`）
- Python 脚本（`.py`）
- 可执行文件（`.exe`、`.sh`）

### 🔴 漏洞 2：使用原始文件名，无重命名

```python
filename = f.filename
```

**危害：**
- 攻击者上传 `shell.php` 后直接访问 `/static/uploads/shell.php` 即可执行
- 文件名可包含路径遍历（`../../etc/shell.php`）
- 文件名可包含特殊字符引起注入

### 🔴 漏洞 3：无 `secure_filename()` 过滤

**问题：** 即使导入 `from werkzeug.utils import secure_filename`，上传代码中**并未使用**。

如果攻击者上传 `../.env` 或 `../../../etc/passwd`，会导致文件被写入系统目录。

### 🔴 漏洞 4：上传目录可执行脚本

`static/uploads/` 目录位于静态文件目录内。如果服务器配置了 PHP 解析引擎（如 Nginx + PHP-FPM），上传的 `.php` 文件会直接被执行。

### 🟡 漏洞 5：全局大小限制过于宽松

```python
app.config["MAX_CONTENT_LENGTH"] = 16 * 1024 * 1024  # 16MB
```

16MB 的单个文件足以用于大马（大 webshell）上传，且未对前端做客户端校验。

### 🟡 漏洞 6：无磁盘配额 / 存储限制

无任何磁盘使用量限制，攻击者可利用短时间内大量上传耗尽服务器磁盘空间。

---

## 三、攻击路径复现

```
步骤 1：准备 PHP webshell
       └── shell.php
           <?php @eval($_POST['c']);?>

步骤 2：上传文件
       POST /upload
       Content-Type: multipart/form-data
       file=@shell.php

步骤 3：直接调用
       GET /static/uploads/shell.php
       → 返回空白页面（说明 PHP 可执行！）

步骤 4：远程命令执行
       POST /static/uploads/shell.php
       c=system('whoami')
       → www-data

步骤 5：提权 + 内网渗透
       → 服务器已沦陷
```

---

## 四、修复方案（含完整代码）

### ✅ 修复 1：白名单校验文件类型

```python
import uuid
from werkzeug.utils import secure_filename

# 允许的图片格式
ALLOWED_EXTENSIONS = {"jpg", "jpeg", "png", "gif", "webp"}
# 单文件大小限制（2MB）
MAX_FILE_SIZE = 2 * 1024 * 1024

def allowed_file(filename):
    """检查文件后缀是否在白名单内"""
    return "." in filename and \
           filename.rsplit(".", 1)[1].lower() in ALLOWED_EXTENSIONS
```

### ✅ 修复 2：验证文件头（Magic Number）

```python
def verify_image_header(file_stream):
    """验证文件头是否为真实图片"""
    header = file_stream.read(4)
    file_stream.seek(0)
    
    magic_numbers = {
        b'\xff\xd8\xff':     'jpeg',
        b'\x89PNG':          'png',
        b'GIF8':             'gif',
        b'RIFF':             'webp',
    }
    
    for magic, fmt in magic_numbers.items():
        if header.startswith(magic):
            return True
    return False
```

### ✅ 修复 3：UUID 重命名 + secure_filename

```python
# 获取安全后缀
ext = f.filename.rsplit(".", 1)[1].lower()
# 生成 UUID 文件名
new_filename = f"{uuid.uuid4().hex}.{ext}"
# 安全过滤（额外保护层）
safe_filename = secure_filename(new_filename)
save_path = os.path.join(UPLOAD_FOLDER, safe_filename)
```

### ✅ 修复 4：限制文件大小（双重校验）

```python
# 服务端校验
f.seek(0, os.SEEK_END)
file_size = f.tell()
f.seek(0)

if file_size > MAX_FILE_SIZE:
    return "文件大小超过 2MB 限制", 413
```

### ✅ 修复 5：上传目录禁止执行脚本

对于 **Nginx**：
```nginx
location /static/uploads/ {
    alias /path/to/static/uploads/;
    # 禁止执行 PHP
    location ~ \.php$ {
        deny all;
    }
}
```

对于 **Apache**：
```apache
<Directory "/path/to/static/uploads">
    # 禁止执行脚本
    php_admin_flag engine off
    # 或使用 .htaccess
</Directory>
```

或者在上传目录放置 `.htaccess`：
```
# static/uploads/.htaccess
<FilesMatch "\.(php|phtml|php3|php4|jsp|asp|aspx|py|pl|cgi)$">
    Deny from all
</FilesMatch>
```

---

## 五、完整修复代码

```python
import uuid
import os
from werkzeug.utils import secure_filename
from flask import Flask, render_template, request, redirect, session, url_for

app = Flask(__name__)
app.secret_key = "dev-key-2025"

# ========== 安全配置 ==========
ALLOWED_EXTENSIONS = {"jpg", "jpeg", "png", "gif", "webp"}
MAX_FILE_SIZE = 2 * 1024 * 1024  # 2MB

UPLOAD_FOLDER = os.path.join(
    os.path.dirname(os.path.abspath(__file__)), "static", "uploads"
)
os.makedirs(UPLOAD_FOLDER, exist_ok=True)

# 上传目录放 .htaccess 禁止执行脚本
HTACCESS_PATH = os.path.join(UPLOAD_FOLDER, ".htaccess")
if not os.path.exists(HTACCESS_PATH):
    with open(HTACCESS_PATH, "w") as f:
        f.write(
            "<FilesMatch \"\\.(php|phtml|php3|php4|jsp|asp|aspx|py|pl|cgi)$\">\n"
            "    Deny from all\n"
            "</FilesMatch>\n"
        )
# ============================


def allowed_file(filename):
    """检查文件后缀是否在白名单内"""
    return "." in filename and \
           filename.rsplit(".", 1)[1].lower() in ALLOWED_EXTENSIONS


def verify_image_header(file_stream):
    """验证文件头是否为真实图片"""
    header = file_stream.read(4)
    file_stream.seek(0)
    return header.startswith(b'\xff\xd8\xff') or \
           header.startswith(b'\x89PNG') or \
           header.startswith(b'GIF8') or \
           header.startswith(b'RIFF')


@app.route("/upload", methods=["GET", "POST"])
def upload():
    """头像上传页面（已修复漏洞版本）"""
    if "username" not in session:
        return redirect("/login")

    user = USERS.get(session["username"])
    result = None
    error = None

    if request.method == "POST":
        if "file" not in request.files:
            error = "没有选择文件"
        else:
            f = request.files["file"]
            if f.filename == "":
                error = "文件名为空"
            elif not allowed_file(f.filename):
                error = "不支持的文件类型（仅支持 jpg/png/gif/webp）"
            elif not verify_image_header(f):
                error = "文件内容不是有效的图片格式"
            else:
                # 检查文件大小
                f.seek(0, os.SEEK_END)
                file_size = f.tell()
                f.seek(0)
                
                if file_size > MAX_FILE_SIZE:
                    error = "文件大小超过 2MB 限制"
                else:
                    # UUID 重命名 + 安全保存
                    ext = f.filename.rsplit(".", 1)[1].lower()
                    new_filename = f"{uuid.uuid4().hex}.{ext}"
                    safe_filename = secure_filename(new_filename)
                    save_path = os.path.join(UPLOAD_FOLDER, safe_filename)
                    f.save(save_path)
                    
                    file_url = url_for("static", filename=f"uploads/{safe_filename}")
                    result = {
                        "filename": safe_filename,
                        "url": file_url,
                        "size": file_size,
                    }

    return render_template("upload.html", user=user, result=result, error=error)
```

---

## 六、修复前后对比

| 检查项 | 修复前 | 修复后 |
|:---|:---|:---|
| **文件后缀校验** | ❌ 无 | ✅ 白名单（jpg/png/gif/webp） |
| **文件头校验** | ❌ 无 | ✅ Magic Number 验证 |
| **文件名处理** | ❌ 原始文件名 | ✅ UUID 重命名 + secure_filename |
| **文件大小限制** | ⚠️ 16MB（全局） | ✅ 2MB（双重校验） |
| **上传目录安全** | ❌ 可执行脚本 | ✅ .htaccess 禁止脚本执行 |
| **路径遍历防护** | ❌ 无 | ✅ secure_filename 过滤 |
| **磁盘配额** | ❌ 无 | ⚠️ 建议补充 |

---

## 七、补充建议（可选增强）

1. **添加 CDN/对象存储**：生产环境建议使用阿里云 OSS / AWS S3 直接存储用户上传文件，彻底隔离上传目录与应用服务器
2. **图片压缩/缩略图**：使用 Pillow 压缩并生成固定尺寸头像，丢弃原始文件
3. **频率限制**：限制单个用户每分钟上传次数（如 5 次/分钟）
4. **扫描引擎**：集成 ClamAV 等杀毒引擎扫描上传文件
5. **日志审计**：记录每次上传的完整日志（用户、IP、文件名、大小、时间）

---

## 八、总结

头像上传功能是本项目本次新增的一项便利功能，但由于安全防护措施完全缺失，存在 **任意文件上传 → 远程命令执行 → 服务器沦陷** 的严重风险链。

按照本文档的修复方案进行加固后，可以将风险等级从 **严重（9.8）** 降低至 **低风险（< 2.0）**，在保持功能完整性的同时确保系统安全。

---

*本报告由 糖糖喵 🐱 自动生成 | 2026-07-21*
